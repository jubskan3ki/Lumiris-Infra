#!/usr/bin/env bash
set -euo pipefail
echo "[seed-local] Le seed n'est pas encore implémenté."
echo "[seed-local] Cette intégration arrivera au Prompt 2 (backend Spring Boot)."
echo "[seed-local] Pour l'instant, le backend gère ses propres seeds via Flyway."
exit 0
